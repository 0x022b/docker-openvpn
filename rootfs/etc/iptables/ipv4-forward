*filter

# Accept active connections coming from tunnel
-A FORWARD -i tun0 -o eth0 -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT

# Accept new and active connection going into tunnel
-A FORWARD -i eth0 -o tun0 -m conntrack --ctstate NEW,RELATED,ESTABLISHED -j ACCEPT

COMMIT

*nat

# Enable dynamic source network address translation
-A POSTROUTING -j MASQUERADE

COMMIT
